To run the application
======================

    $ make run-iphone
    $ make clean && make run-ipad

To run the application executing the tests
======================

    $ make test-iphone
    $ make test-ipad

